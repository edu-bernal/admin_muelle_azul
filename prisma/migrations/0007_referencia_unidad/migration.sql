-- Nota libre de la unidad. Nace por las cocheras: su código es correlativo
-- y no distingue una de otra, así que hacía falta dónde guardar la referencia
-- que ya se pedía en el formulario.
ALTER TABLE "Unidad" ADD COLUMN "referencia" TEXT;
